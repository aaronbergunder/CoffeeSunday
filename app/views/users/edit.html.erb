<div class="row"><h1>Editing user</h1></div>

<%= render 'form' %>

<%= link_to 'Show', @user, :class => "button" %> 
<%= link_to 'Back', users_path, :class => "button" %>

<%= link_to 'Delete User', @user, :class => "button", method: :delete, data: { confirm: 'Are you sure?' } %>